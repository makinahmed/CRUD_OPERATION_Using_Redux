import React from "react";

export default function TopRated() {
  return (
    <div
      style={{
        display: "flex",
        flexWrap: "wrap",
        justifyContent: "space-evenly",
        gap: "15px",
        marginTop: "200px",
      }}
    >
      Top Rated
    </div>
  );
}
